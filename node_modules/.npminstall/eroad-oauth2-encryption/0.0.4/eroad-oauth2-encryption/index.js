const encrypted = require('./lib/encrypt');
const decrypted = require('./lib/decrypt');

class Cipher {

    static decrypt(data, key, salt) {
        return decrypted(data, key, salt);
    }

    static encrypt(data, key, salt) {
        return encrypted(data, key, salt);
    }

}

module.exports = Cipher;